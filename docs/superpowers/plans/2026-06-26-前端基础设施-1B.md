# 前端基础设施 1B 实现计划

- **版本**: v1
- **日期**: 2026-06-26
- **状态**: 待执行
- **分支**: feature/frontend-1B
- **前置**: 后端基础设施 1A 已合并到 main(登录/用户/角色/菜单/部门接口可用)
- **关联**: [UI 设计规范](../specs/2026-06-26-UI设计规范-design.md)、[需求文档](../../../需求文档.md)

---

## 目标

搭建 Vue 3 前端骨架,落地 Linear/Notion 风主题,完成登录页 + 主布局 + 路由权限 + 系统管理 4 个页面(用户/角色/菜单/部门),与后端 1A 接口联调通过。

## 技术栈

Vue 3.4 + Vite 5 + TypeScript + Element Plus 2 + Pinia + Vue Router 4 + Axios + SCSS + Lucide Icons + Unplugin Auto Import

## 验收标准

1. `pnpm dev` 启动成功,`pnpm build` 产物构建通过
2. admin/admin123 登录跳转主页,token 存 localStorage,刷新不丢失
3. 未登录访问受保护路由 → 重定向登录页
4. 侧栏菜单由后端 `/sys/menu/userMenus` 动态渲染,点击可路由
5. 用户/角色/菜单/部门 4 页 CRUD 与后端联调通过
6. 视觉落地 UI 规范:主色 #4F6BED、圆角 6px、表格行 hover、侧栏选中态、卡片边框分层

---

## 任务 1:项目初始化与依赖

**文件:** `web/` 目录

- [ ] **步骤 1:** 在仓库根目录创建 Vite + Vue3 + TS 项目
  ```powershell
  pnpm create vite web --template vue-ts
  ```
- [ ] **步骤 2:** 安装依赖
  ```powershell
  cd web
  pnpm add element-plus @element-plus/icons-vue pinia vue-router@4 axios lucide-vue-next
  pnpm add -D sass unplugin-auto-import unplugin-vue-components @types/node
  ```
- [ ] **步骤 3:** 配置 `vite.config.ts`(端口 5173、代理 /api 到 8080、Element Plus 自动按需、路径别名 @)
  ```ts
  import { defineConfig } from 'vite'
  import vue from '@vitejs/plugin-vue'
  import AutoImport from 'unplugin-auto-import/vite'
  import Components from 'unplugin-vue-components/vite'
  import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'
  import path from 'path'

  export default defineConfig({
    plugins: [
      vue(),
      AutoImport({ resolvers: [ElementPlusResolver()] }),
      Components({ resolvers: [ElementPlusResolver()] }),
    ],
    resolve: { alias: { '@': path.resolve(__dirname, 'src') } },
    server: {
      port: 5173,
      proxy: { '/api': { target: 'http://localhost:8080', changeOrigin: true } },
    },
  })
  ```
- [ ] **步骤 4:** 验证 `pnpm dev` 启动成功
- [ ] **步骤 5:** Commit `chore: 前端项目初始化(任务1/12)`

---

## 任务 2:SCSS 主题变量与全局样式

**文件:** `src/styles/variables.scss`、`src/styles/element-overrides.scss`、`src/styles/global.scss`、`src/main.ts`

- [ ] **步骤 1:** 编写 `variables.scss`(对齐 UI 规范第二节配色)
  ```scss
  $color-primary: #4F6BED;
  $color-primary-hover: #3D5BDB;
  $color-primary-active: #2C49C9;
  $text-primary: #1F2937;
  $text-regular: #4B5563;
  $text-secondary: #9CA3AF;
  $border-color: #E5E7EB;
  $divider-color: #F3F4F6;
  $bg-page: #FFFFFF;
  $bg-sidebar: #FAFAFA;
  $bg-hover: #F9FAFB;
  $success: #10B981; $warning: #F59E0B; $danger: #EF4444; $info: #3B82F6;
  $radius-btn: 6px; $radius-card: 8px; $radius-modal: 12px; $radius-tag: 4px;
  $shadow-card: 0 1px 2px rgba(0,0,0,0.04);
  $shadow-hover: 0 4px 12px rgba(0,0,0,0.08);
  $shadow-modal: 0 8px 24px rgba(0,0,0,0.12);
  $font-family: 'Inter', 'PingFang SC', sans-serif;
  ```
- [ ] **步骤 2:** 编写 `element-overrides.scss`(覆盖 Element Plus 变量,需在引入前)
  ```scss
  @forward 'element-plus/theme-chalk/src/common/var.scss' with (
    $colors: ( 'primary': ('base': #4F6BED) ),
    $text-color: ( 'primary': #1F2937, 'regular': #4B5563, 'secondary': #9CA3AF ),
    $border-color: ( 'base': #E5E7EB, 'light': #F3F4F6 ),
    $border-radius: ( 'base': 6px, 'small': 4px ),
    $font-size: ( 'base': 14px ),
    $font-family: ('Inter', 'PingFang SC', sans-serif),
  );
  ```
- [ ] **步骤 3:** 编写 `global.scss`(全局重置、字体、滚动条)
- [ ] **步骤 4:** 在 `main.ts` 中按顺序引入:`element-overrides.scss` → `element-plus/dist/index.css` → `global.scss`
- [ ] **步骤 5:** 验证启动无样式报错
- [ ] **步骤 6:** Commit `feat: SCSS 主题变量与全局样式(任务2/12)`

---

## 任务 3:Axios 封装与请求/响应拦截

**文件:** `src/utils/request.ts`

- [ ] **步骤 1:** 编写 request.ts
  ```ts
  import axios, { type InternalAxiosRequestConfig, type AxiosResponse } from 'axios'
  import { ElMessage } from 'element-plus'
  import router from '@/router'

  const service = axios.create({ baseURL: '/api', timeout: 15000 })

  service.interceptors.request.use((config: InternalAxiosRequestConfig) => {
    const token = localStorage.getItem('ems_token')
    if (token) config.headers.Authorization = `Bearer ${token}`
    return config
  })

  service.interceptors.response.use(
    (response: AxiosResponse) => {
      const { code, message, data } = response.data
      if (code === 200) return data
      if (code === 401) {
        ElMessage.error(message || '未登录')
        localStorage.removeItem('ems_token')
        router.push('/login')
        return Promise.reject(new Error(message))
      }
      ElMessage.error(message || '请求失败')
      return Promise.reject(new Error(message))
    },
    (error) => {
      ElMessage.error(error.message || '网络错误')
      return Promise.reject(error)
    }
  )

  export default service
  ```
- [ ] **步骤 2:** Commit `feat: Axios 封装(任务3/12)`

---

## 任务 4:Pinia 用户 Store 与 API 模块

**文件:** `src/stores/user.ts`、`src/api/auth.ts`、`src/api/system.ts`

- [ ] **步骤 1:** 编写 `api/auth.ts`(login、logout)
- [ ] **步骤 2:** 编写 `api/system.ts`(user/role/menu/dept 的 page/CRUD/分配接口)
- [ ] **步骤 3:** 编写 `stores/user.ts`
  ```ts
  import { defineStore } from 'pinia'
  import { ref } from 'vue'
  import { login as loginApi, logout as logoutApi } from '@/api/auth'
  import { getUserMenus } from '@/api/system'

  export const useUserStore = defineStore('user', () => {
    const token = ref(localStorage.getItem('ems_token') || '')
    const userId = ref<number | null>(null)
    const username = ref('')
    const name = ref('')
    const menus = ref<any[]>([])

    async function login(loginForm: { username: string; password: string }) {
      const data = await loginApi(loginForm)
      token.value = data.token
      userId.value = data.userId
      username.value = data.username
      name.value = data.name
      localStorage.setItem('ems_token', data.token)
    }

    async function loadMenus() {
      menus.value = await getUserMenus()
      return menus.value
    }

    async function logout() {
      try { await logoutApi() } catch {}
      token.value = ''; menus.value = []
      localStorage.removeItem('ems_token')
    }

    return { token, userId, username, name, menus, login, loadMenus, logout }
  })
  ```
- [ ] **步骤 4:** Commit `feat: 用户 Store 与 API 模块(任务4/12)`

---

## 任务 5:路由与权限守卫

**文件:** `src/router/index.ts`

- [ ] **步骤 1:** 定义静态路由(login、404、根布局 children 占位)
- [ ] **步骤 2:** 编写全局前置守卫:无 token → /login;有 token 但无 menus → loadMenus + 动态 addRoute;已登录访问 /login → 重定向 /
- [ ] **步骤 3:** 动态路由生成:根据 menu.path + menu.name 注册 component(用 import.meta.glob 预加载 views/system 下页面)
- [ ] **步骤 4:** Commit `feat: 路由与权限守卫(任务5/12)`

---

## 任务 6:登录页

**文件:** `src/views/login/index.vue`

- [ ] **步骤 1:** 编写登录页(居中卡片、Logo、用户名/密码输入、登录按钮、回车提交)
- [ ] **步骤 2:** 表单校验(非空),调用 userStore.login,成功后 router.push('/')
- [ ] **步骤 3:** 视觉对齐 UI 规范:卡片圆角 8px、阴影 modal 级、输入框聚焦发光、主色按钮
- [ ] **步骤 4:** 验证 admin/admin123 登录跳转主页
- [ ] **步骤 5:** Commit `feat: 登录页(任务6/12)`

---

## 任务 7:主布局(侧栏 + 顶栏 + 内容区)

**文件:** `src/layout/index.vue`、`src/layout/components/Sidebar.vue`、`src/layout/components/Header.vue`

- [ ] **步骤 1:** 主布局三栏:左侧 240px 侧栏、顶部 56px 顶栏、右侧内容区 padding 24px
- [ ] **步骤 2:** Sidebar:递归渲染 menus,Lucide 图标(按 menu.icon 映射),选中态主色 10% 透明背景 + 主色字,hover 背景 #F3F4F6
- [ ] **步骤 3:** Header:左侧折叠按钮 + 面包屑,右侧用户头像 + 下拉(退出登录)
- [ ] **步骤 4:** Commit `feat: 主布局(任务7/12)`

---

## 任务 8:通用 CRUD 组件抽象

**文件:** `src/components/PageTable.vue`、`src/components/FormDialog.vue`

- [ ] **步骤 1:** PageTable:封装 el-table + el-pagination,props 接收 columns/data/total/loading,emit page-change
- [ ] **步骤 2:** FormDialog:封装 el-dialog + el-form,props 接收 visible/title/fields,emit submit/close
- [ ] **步骤 3:** Commit `feat: 通用表格与表单弹窗组件(任务8/12)`

---

## 任务 9:用户管理页

**文件:** `src/views/system/user/index.vue`

- [ ] **步骤 1:** 搜索栏(姓名 + 部门选择)+ 表格(用户名/姓名/部门/状态/操作)+ 新增/编辑弹窗 + 分配角色弹窗
- [ ] **步骤 2:** 调用 /sys/user/page、create、update、delete、assignRoles、roleIds、/sys/dept/tree
- [ ] **步骤 3:** 验证联调:分页/新增/编辑/删除/分配角色全流程
- [ ] **步骤 4:** Commit `feat: 用户管理页(任务9/12)`

---

## 任务 10:角色管理页

**文件:** `src/views/system/role/index.vue`

- [ ] **步骤 1:** 表格(角色名/编码/数据范围/状态/操作)+ 新增/编辑弹窗 + 分配菜单弹窗(el-tree 勾选)
- [ ] **步骤 2:** 调用 /sys/role/page、create、update、delete、menuIds、/sys/menu/tree
- [ ] **步骤 3:** Commit `feat: 角色管理页(任务10/12)`

---

## 任务 11:菜单管理页 + 部门管理页

**文件:** `src/views/system/menu/index.vue`、`src/views/system/dept/index.vue`

- [ ] **步骤 1:** 菜单页:el-table 树形展示(row-key=id,tree-props),列:名称/图标/类型/权限/路径/排序/操作,新增/编辑弹窗
- [ ] **步骤 2:** 部门页:同上树形,列:名称/负责人/排序/状态/操作
- [ ] **步骤 3:** Commit `feat: 菜单与部门管理页(任务11/12)`

---

## 任务 12:最终验证与构建

- [ ] **步骤 1:** `pnpm dev` 启动,全流程验证:登录 → 菜单渲染 → 4 页 CRUD → 退出
- [ ] **步骤 2:** `pnpm build` 构建产物,确认无 TS 错误
- [ ] **步骤 3:** 视觉走查:主色/圆角/阴影/hover/选中态符合 UI 规范
- [ ] **步骤 4:** Commit `chore: 前端 1B 验证通过(任务12/12)`
- [ ] **步骤 5:** 推送 feature/frontend-1B,合并到 main

---

## 风险与备忘

- **Element Plus 主题覆盖**:必须用 SCSS @forward 在引入 element-plus 前覆盖,否则不生效
- **动态路由刷新丢失**:需在 router/index.ts 顶部判断 token 并预加载 menus,避免刷新白屏
- **代理路径**:前端 /api 代理到后端 8080,后端 context-path 也是 /api,最终请求路径为 /api/xxx(无重复)
- **图标映射**:menu.icon 存的是字符串(如 'User'),需建 iconMap 映射到 Lucide 组件
