# 业务模块 2A 实现计划(基础能力层 - 合同/报价/项目/设备)

> **面向 AI 代理的工作者:** 必需子技能:使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 实现项目基类、合同管理、报价上传存储、设备台账 4 个基础能力模块的后端 CRUD 接口,为后续业务编排层(2B/2C)打基础。

**架构：** 遵循需求文档第五节分层架构。本计划只做"基础能力层"实体,不实现维护型项目编排(2B)、季度结算定时任务(2C)。实体通过 `business_type` 字段预留扩展,但 2A 仅实现 NEW_BUILD 场景的 CRUD。

**技术栈：** Spring Boot 3.2 + MyBatis Plus 3.5 + MySQL 8,沿用 1A 的 BaseEntity/Result/PageResult/权限体系。

**前置：** 后端基础设施 1A 已合并到 main(用户/角色/菜单/部门 + JWT + 权限注解)。

**分支：** feature/business-2A

---

## 文件结构

### 数据库
- 修改：`server/src/main/resources/db/schema.sql` — 新增 4 张业务表

### 实体(entity)
- 创建：`server/src/main/java/com/ems/module/business/entity/Project.java` — 项目基类(NEW_BUILD/MAINTENANCE)
- 创建：`server/src/main/java/com/ems/module/business/entity/Contract.java` — 合同
- 创建：`server/src/main/java/com/ems/module/business/entity/Quote.java` — 报价(含 business_type)
- 创建：`server/src/main/java/com/ems/module/business/entity/Equipment.java` — 设备台账

### Mapper
- 创建：`server/src/main/java/com/ems/module/business/mapper/ProjectMapper.java`
- 创建：`server/src/main/java/com/ems/module/business/mapper/ContractMapper.java`
- 创建：`server/src/main/java/com/ems/module/business/mapper/QuoteMapper.java`
- 创建：`server/src/main/java/com/ems/module/business/mapper/EquipmentMapper.java`

### DTO
- 创建：`server/src/main/java/com/ems/module/business/dto/ProjectDTO.java`
- 创建：`server/src/main/java/com/ems/module/business/dto/ContractDTO.java`
- 创建：`server/src/main/java/com/ems/module/business/dto/QuoteDTO.java`
- 创建：`server/src/main/java/com/ems/module/business/dto/EquipmentDTO.java`

### Service
- 创建：`server/src/main/java/com/ems/module/business/service/ProjectService.java`
- 创建：`server/src/main/java/com/ems/module/business/service/ContractService.java`
- 创建：`server/src/main/java/com/ems/module/business/service/QuoteService.java`
- 创建：`server/src/main/java/com/ems/module/business/service/EquipmentService.java`

### Controller
- 创建：`server/src/main/java/com/ems/module/business/controller/ProjectController.java`
- 创建：`server/src/main/java/com/ems/module/business/controller/ContractController.java`
- 创建：`server/src/main/java/com/ems/module/business/controller/QuoteController.java`
- 创建：`server/src/main/java/com/ems/module/business/controller/EquipmentController.java`

---

## 任务 1:数据库表结构与种子数据

**文件：**
- 修改：`server/src/main/resources/db/schema.sql`

- [ ] **步骤 1:在 schema.sql 末尾追加 4 张业务表**

```sql
-- ===== 业务模块:项目基类 =====
CREATE TABLE IF NOT EXISTS project (
    id BIGINT PRIMARY KEY,
    code VARCHAR(50) NOT NULL COMMENT '项目编号',
    name VARCHAR(200) NOT NULL COMMENT '项目名称',
    customer_name VARCHAR(200) COMMENT '客户名称(简化,无客户表)',
    manager_id BIGINT COMMENT '项目经理(user.id)',
    address VARCHAR(500) COMMENT '项目地址',
    start_date DATE COMMENT '开工日期',
    end_date DATE COMMENT '竣工日期',
    type VARCHAR(20) NOT NULL DEFAULT 'NEW_BUILD' COMMENT 'NEW_BUILD新建工程/MAINTENANCE维护型',
    status VARCHAR(20) NOT NULL DEFAULT 'DRAFT' COMMENT 'DRAFT/IN_PROGRESS/COMPLETED/ARCHIVED',
    description VARCHAR(1000) COMMENT '项目描述',
    create_by BIGINT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0,
    UNIQUE KEY uk_code (code),
    INDEX idx_type (type),
    INDEX idx_status (status)
) COMMENT '项目表';

-- ===== 业务模块:合同管理 =====
CREATE TABLE IF NOT EXISTS contract (
    id BIGINT PRIMARY KEY,
    code VARCHAR(50) NOT NULL COMMENT '合同编号',
    name VARCHAR(200) NOT NULL COMMENT '合同名称',
    party_a VARCHAR(200) COMMENT '甲方',
    party_b VARCHAR(200) COMMENT '乙方',
    sign_date DATE COMMENT '签订日期',
    amount DECIMAL(14,2) DEFAULT 0 COMMENT '合同金额',
    category VARCHAR(20) COMMENT 'GENERAL总包/SUB分包/PURCHASE采购/MAINTENANCE维保',
    payment_method VARCHAR(200) COMMENT '付款方式',
    project_id BIGINT COMMENT '关联项目',
    status VARCHAR(20) NOT NULL DEFAULT 'DRAFT' COMMENT 'DRAFT/APPROVING/APPROVED/ARCHIVED',
    start_date DATE COMMENT '合同生效日',
    end_date DATE COMMENT '合同到期日',
    remark VARCHAR(500) COMMENT '备注',
    create_by BIGINT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0,
    UNIQUE KEY uk_code (code),
    INDEX idx_project (project_id),
    INDEX idx_status (status)
) COMMENT '合同表';

-- ===== 业务模块:报价管理(基础能力层,含 business_type) =====
CREATE TABLE IF NOT EXISTS quote (
    id BIGINT PRIMARY KEY,
    code VARCHAR(50) NOT NULL COMMENT '报价编号',
    project_id BIGINT COMMENT '关联项目',
    business_type VARCHAR(30) NOT NULL DEFAULT 'NEW_BUILD' COMMENT 'NEW_BUILD/MAINTENANCE_POINT',
    business_id BIGINT COMMENT '业务ID(NEW_BUILD=project_id, MAINTENANCE_POINT=point_id)',
    amount DECIMAL(14,2) DEFAULT 0 COMMENT '报价金额',
    quote_date DATE COMMENT '报价日期',
    valid_until DATE COMMENT '报价有效期',
    quote_person VARCHAR(100) COMMENT '报价人',
    customer_name VARCHAR(200) COMMENT '客户名称',
    version INT DEFAULT 1 COMMENT '版本号',
    status VARCHAR(20) NOT NULL DEFAULT 'DRAFT' COMMENT 'DRAFT/SUBMITTED/APPROVED/CONFIRMED/VOID',
    summary VARCHAR(1000) COMMENT '报价摘要',
    create_by BIGINT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0,
    UNIQUE KEY uk_code (code),
    INDEX idx_project (project_id),
    INDEX idx_business (business_type, business_id)
) COMMENT '报价表';

-- ===== 业务模块:设备台账 =====
CREATE TABLE IF NOT EXISTS equipment (
    id BIGINT PRIMARY KEY,
    code VARCHAR(50) NOT NULL COMMENT '设备编号',
    name VARCHAR(200) NOT NULL COMMENT '设备名称',
    brand VARCHAR(100) COMMENT '品牌',
    model VARCHAR(100) COMMENT '型号',
    serial_number VARCHAR(100) COMMENT '序列号',
    category VARCHAR(50) COMMENT '分类:门禁/监控/网络/楼控等',
    specs VARCHAR(500) COMMENT '规格参数',
    commissioning_date DATE COMMENT '投运日期',
    warranty_expiry DATE COMMENT '保修到期日',
    status VARCHAR(20) NOT NULL DEFAULT 'NORMAL' COMMENT 'NORMAL正常/FAULT故障/REPAIRING维修中/SCRAPPED报废',
    project_id BIGINT COMMENT '归属项目',
    point_id BIGINT COMMENT '归属点位(维护型项目,2A不填)',
    create_by BIGINT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0,
    UNIQUE KEY uk_code (code),
    INDEX idx_project (project_id),
    INDEX idx_status (status)
) COMMENT '设备台账表';
```

- [ ] **步骤 2:在数据库执行新表 DDL**

运行(连接 192.168.12.157:19980):
```sql
-- 依次执行上述 4 段 CREATE TABLE
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/resources/db/schema.sql
git commit -m "feat: 业务表 DDL(项目/合同/报价/设备)"
```

---

## 任务 2:项目实体与 Mapper

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/entity/Project.java`
- 创建：`server/src/main/java/com/ems/module/business/mapper/ProjectMapper.java`

- [ ] **步骤 1:创建 Project 实体**

```java
package com.ems.module.business.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.ems.common.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.time.LocalDate;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("project")
public class Project extends BaseEntity {
    private String code;
    private String name;
    private String customerName;
    private Long managerId;
    private String address;
    private LocalDate startDate;
    private LocalDate endDate;
    private String type;       // NEW_BUILD / MAINTENANCE
    private String status;     // DRAFT / IN_PROGRESS / COMPLETED / ARCHIVED
    private String description;
}
```

- [ ] **步骤 2:创建 ProjectMapper**

```java
package com.ems.module.business.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.ems.module.business.entity.Project;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface ProjectMapper extends BaseMapper<Project> {
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/
git commit -m "feat: 项目实体与 Mapper"
```

---

## 任务 3:合同实体与 Mapper

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/entity/Contract.java`
- 创建：`server/src/main/java/com/ems/module/business/mapper/ContractMapper.java`

- [ ] **步骤 1:创建 Contract 实体**

```java
package com.ems.module.business.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.ems.common.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.math.BigDecimal;
import java.time.LocalDate;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("contract")
public class Contract extends BaseEntity {
    private String code;
    private String name;
    private String partyA;
    private String partyB;
    private LocalDate signDate;
    private BigDecimal amount;
    private String category;       // GENERAL / SUB / PURCHASE / MAINTENANCE
    private String paymentMethod;
    private Long projectId;
    private String status;         // DRAFT / APPROVING / APPROVED / ARCHIVED
    private LocalDate startDate;
    private LocalDate endDate;
    private String remark;
}
```

- [ ] **步骤 2:创建 ContractMapper**

```java
package com.ems.module.business.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.ems.module.business.entity.Contract;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface ContractMapper extends BaseMapper<Contract> {
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/
git commit -m "feat: 合同实体与 Mapper"
```

---

## 任务 4:报价实体与 Mapper

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/entity/Quote.java`
- 创建：`server/src/main/java/com/ems/module/business/mapper/QuoteMapper.java`

- [ ] **步骤 1:创建 Quote 实体**

```java
package com.ems.module.business.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.ems.common.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.math.BigDecimal;
import java.time.LocalDate;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("quote")
public class Quote extends BaseEntity {
    private String code;
    private Long projectId;
    private String businessType;   // NEW_BUILD / MAINTENANCE_POINT
    private Long businessId;
    private BigDecimal amount;
    private LocalDate quoteDate;
    private LocalDate validUntil;
    private String quotePerson;
    private String customerName;
    private Integer version;
    private String status;         // DRAFT / SUBMITTED / APPROVED / CONFIRMED / VOID
    private String summary;
}
```

- [ ] **步骤 2:创建 QuoteMapper**

```java
package com.ems.module.business.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.ems.module.business.entity.Quote;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface QuoteMapper extends BaseMapper<Quote> {
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/
git commit -m "feat: 报价实体与 Mapper"
```

---

## 任务 5:设备台账实体与 Mapper

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/entity/Equipment.java`
- 创建：`server/src/main/java/com/ems/module/business/mapper/EquipmentMapper.java`

- [ ] **步骤 1:创建 Equipment 实体**

```java
package com.ems.module.business.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.ems.common.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.time.LocalDate;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("equipment")
public class Equipment extends BaseEntity {
    private String code;
    private String name;
    private String brand;
    private String model;
    private String serialNumber;
    private String category;
    private String specs;
    private LocalDate commissioningDate;
    private LocalDate warrantyExpiry;
    private String status;     // NORMAL / FAULT / REPAIRING / SCRAPPED
    private Long projectId;
    private Long pointId;
}
```

- [ ] **步骤 2:创建 EquipmentMapper**

```java
package com.ems.module.business.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.ems.module.business.entity.Equipment;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface EquipmentMapper extends BaseMapper<Equipment> {
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/
git commit -m "feat: 设备台账实体与 Mapper"
```

---

## 任务 6:DTO 类

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/dto/ProjectDTO.java`
- 创建：`server/src/main/java/com/ems/module/business/dto/ContractDTO.java`
- 创建：`server/src/main/java/com/ems/module/business/dto/QuoteDTO.java`
- 创建：`server/src/main/java/com/ems/module/business/dto/EquipmentDTO.java`

- [ ] **步骤 1:创建 4 个 DTO**

```java
// ProjectDTO.java
package com.ems.module.business.dto;

import jakarta.validation.constraints.NotBlank;
import lombok.Data;

@Data
public class ProjectDTO {
    private Long id;
    @NotBlank(message = "项目编号不能为空")
    private String code;
    @NotBlank(message = "项目名称不能为空")
    private String name;
    private String customerName;
    private Long managerId;
    private String address;
    private String startDate;
    private String endDate;
    private String type;
    private String status;
    private String description;
}
```

```java
// ContractDTO.java
package com.ems.module.business.dto;

import jakarta.validation.constraints.NotBlank;
import lombok.Data;
import java.math.BigDecimal;

@Data
public class ContractDTO {
    private Long id;
    @NotBlank(message = "合同编号不能为空")
    private String code;
    @NotBlank(message = "合同名称不能为空")
    private String name;
    private String partyA;
    private String partyB;
    private String signDate;
    private BigDecimal amount;
    private String category;
    private String paymentMethod;
    private Long projectId;
    private String status;
    private String startDate;
    private String endDate;
    private String remark;
}
```

```java
// QuoteDTO.java
package com.ems.module.business.dto;

import jakarta.validation.constraints.NotBlank;
import lombok.Data;
import java.math.BigDecimal;

@Data
public class QuoteDTO {
    private Long id;
    @NotBlank(message = "报价编号不能为空")
    private String code;
    private Long projectId;
    private String businessType;
    private Long businessId;
    private BigDecimal amount;
    private String quoteDate;
    private String validUntil;
    private String quotePerson;
    private String customerName;
    private Integer version;
    private String status;
    private String summary;
}
```

```java
// EquipmentDTO.java
package com.ems.module.business.dto;

import jakarta.validation.constraints.NotBlank;
import lombok.Data;

@Data
public class EquipmentDTO {
    private Long id;
    @NotBlank(message = "设备编号不能为空")
    private String code;
    @NotBlank(message = "设备名称不能为空")
    private String name;
    private String brand;
    private String model;
    private String serialNumber;
    private String category;
    private String specs;
    private String commissioningDate;
    private String warrantyExpiry;
    private String status;
    private Long projectId;
    private Long pointId;
}
```

- [ ] **步骤 2:Commit**

```bash
git add server/src/main/java/com/ems/module/business/dto/
git commit -m "feat: 业务模块 DTO"
```

---

## 任务 7:项目 Service + Controller

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/service/ProjectService.java`
- 创建：`server/src/main/java/com/ems/module/business/controller/ProjectController.java`

- [ ] **步骤 1:创建 ProjectService**

```java
package com.ems.module.business.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.ems.common.PageResult;
import com.ems.common.exception.BusinessException;
import com.ems.module.business.dto.ProjectDTO;
import com.ems.module.business.entity.Project;
import com.ems.module.business.mapper.ProjectMapper;
import com.ems.security.context.SecurityContext;
import org.springframework.beans.BeanUtils;
import org.springframework.stereotype.Service;

import java.time.LocalDate;

@Service
public class ProjectService {
    private final ProjectMapper projectMapper;

    public ProjectService(ProjectMapper projectMapper) {
        this.projectMapper = projectMapper;
    }

    public PageResult<Project> page(long pageNum, long pageSize, String name, String type, String status) {
        LambdaQueryWrapper<Project> wrapper = new LambdaQueryWrapper<Project>()
                .like(name != null && !name.isEmpty(), Project::getName, name)
                .eq(type != null && !type.isEmpty(), Project::getType, type)
                .eq(status != null && !status.isEmpty(), Project::getStatus, status)
                .orderByDesc(Project::getCreateTime);
        IPage<Project> page = projectMapper.selectPage(new Page<>(pageNum, pageSize), wrapper);
        return PageResult.of(page.getRecords(), page.getTotal());
    }

    public Project get(Long id) {
        Project p = projectMapper.selectById(id);
        if (p == null) throw new BusinessException("项目不存在");
        return p;
    }

    public void create(ProjectDTO dto) {
        Project p = new Project();
        BeanUtils.copyProperties(dto, p);
        if (dto.getStartDate() != null) p.setStartDate(LocalDate.parse(dto.getStartDate()));
        if (dto.getEndDate() != null) p.setEndDate(LocalDate.parse(dto.getEndDate()));
        if (p.getType() == null) p.setType("NEW_BUILD");
        if (p.getStatus() == null) p.setStatus("DRAFT");
        p.setCreateBy(SecurityContext.getUserId());
        projectMapper.insert(p);
    }

    public void update(ProjectDTO dto) {
        Project existing = get(dto.getId());
        BeanUtils.copyProperties(dto, existing);
        if (dto.getStartDate() != null) existing.setStartDate(LocalDate.parse(dto.getStartDate()));
        if (dto.getEndDate() != null) existing.setEndDate(LocalDate.parse(dto.getEndDate()));
        projectMapper.updateById(existing);
    }

    public void delete(Long id) {
        get(id);
        projectMapper.deleteById(id);
    }
}
```

- [ ] **步骤 2:创建 ProjectController**

```java
package com.ems.module.business.controller;

import com.ems.common.PageResult;
import com.ems.common.Result;
import com.ems.module.business.dto.ProjectDTO;
import com.ems.module.business.entity.Project;
import com.ems.module.business.service.ProjectService;
import com.ems.security.annotation.RequirePermission;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/business/project")
public class ProjectController {
    private final ProjectService projectService;

    public ProjectController(ProjectService projectService) {
        this.projectService = projectService;
    }

    @GetMapping("/page")
    @RequirePermission("business:project:list")
    public Result<PageResult<Project>> page(@RequestParam(defaultValue = "1") long pageNum,
                                            @RequestParam(defaultValue = "10") long pageSize,
                                            String name, String type, String status) {
        return Result.success(projectService.page(pageNum, pageSize, name, type, status));
    }

    @GetMapping("/{id}")
    @RequirePermission("business:project:list")
    public Result<Project> get(@PathVariable Long id) {
        return Result.success(projectService.get(id));
    }

    @PostMapping
    @RequirePermission("business:project:create")
    public Result<Void> create(@Valid @RequestBody ProjectDTO dto) {
        projectService.create(dto);
        return Result.success();
    }

    @PutMapping
    @RequirePermission("business:project:update")
    public Result<Void> update(@Valid @RequestBody ProjectDTO dto) {
        projectService.update(dto);
        return Result.success();
    }

    @DeleteMapping("/{id}")
    @RequirePermission("business:project:delete")
    public Result<Void> delete(@PathVariable Long id) {
        projectService.delete(id);
        return Result.success();
    }
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/service/ProjectService.java server/src/main/java/com/ems/module/business/controller/ProjectController.java
git commit -m "feat: 项目管理 CRUD"
```

---

## 任务 8:合同 Service + Controller

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/service/ContractService.java`
- 创建：`server/src/main/java/com/ems/module/business/controller/ContractController.java`

- [ ] **步骤 1:创建 ContractService(模式同 ProjectService,字段适配合同)**

```java
package com.ems.module.business.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.ems.common.PageResult;
import com.ems.common.exception.BusinessException;
import com.ems.module.business.dto.ContractDTO;
import com.ems.module.business.entity.Contract;
import com.ems.module.business.mapper.ContractMapper;
import com.ems.security.context.SecurityContext;
import org.springframework.beans.BeanUtils;
import org.springframework.stereotype.Service;

import java.time.LocalDate;

@Service
public class ContractService {
    private final ContractMapper contractMapper;

    public ContractService(ContractMapper contractMapper) {
        this.contractMapper = contractMapper;
    }

    public PageResult<Contract> page(long pageNum, long pageSize, String name, String code, String status) {
        LambdaQueryWrapper<Contract> wrapper = new LambdaQueryWrapper<Contract>()
                .like(name != null && !name.isEmpty(), Contract::getName, name)
                .like(code != null && !code.isEmpty(), Contract::getCode, code)
                .eq(status != null && !status.isEmpty(), Contract::getStatus, status)
                .orderByDesc(Contract::getCreateTime);
        IPage<Contract> page = contractMapper.selectPage(new Page<>(pageNum, pageSize), wrapper);
        return PageResult.of(page.getRecords(), page.getTotal());
    }

    public Contract get(Long id) {
        Contract c = contractMapper.selectById(id);
        if (c == null) throw new BusinessException("合同不存在");
        return c;
    }

    public void create(ContractDTO dto) {
        Contract c = new Contract();
        BeanUtils.copyProperties(dto, c);
        if (dto.getSignDate() != null) c.setSignDate(LocalDate.parse(dto.getSignDate()));
        if (dto.getStartDate() != null) c.setStartDate(LocalDate.parse(dto.getStartDate()));
        if (dto.getEndDate() != null) c.setEndDate(LocalDate.parse(dto.getEndDate()));
        if (c.getStatus() == null) c.setStatus("DRAFT");
        c.setCreateBy(SecurityContext.getUserId());
        contractMapper.insert(c);
    }

    public void update(ContractDTO dto) {
        Contract existing = get(dto.getId());
        BeanUtils.copyProperties(dto, existing);
        if (dto.getSignDate() != null) existing.setSignDate(LocalDate.parse(dto.getSignDate()));
        if (dto.getStartDate() != null) existing.setStartDate(LocalDate.parse(dto.getStartDate()));
        if (dto.getEndDate() != null) existing.setEndDate(LocalDate.parse(dto.getEndDate()));
        contractMapper.updateById(existing);
    }

    public void delete(Long id) {
        get(id);
        contractMapper.deleteById(id);
    }
}
```

- [ ] **步骤 2:创建 ContractController**

```java
package com.ems.module.business.controller;

import com.ems.common.PageResult;
import com.ems.common.Result;
import com.ems.module.business.dto.ContractDTO;
import com.ems.module.business.entity.Contract;
import com.ems.module.business.service.ContractService;
import com.ems.security.annotation.RequirePermission;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/business/contract")
public class ContractController {
    private final ContractService contractService;

    public ContractController(ContractService contractService) {
        this.contractService = contractService;
    }

    @GetMapping("/page")
    @RequirePermission("business:contract:list")
    public Result<PageResult<Contract>> page(@RequestParam(defaultValue = "1") long pageNum,
                                             @RequestParam(defaultValue = "10") long pageSize,
                                             String name, String code, String status) {
        return Result.success(contractService.page(pageNum, pageSize, name, code, status));
    }

    @GetMapping("/{id}")
    @RequirePermission("business:contract:list")
    public Result<Contract> get(@PathVariable Long id) {
        return Result.success(contractService.get(id));
    }

    @PostMapping
    @RequirePermission("business:contract:create")
    public Result<Void> create(@Valid @RequestBody ContractDTO dto) {
        contractService.create(dto);
        return Result.success();
    }

    @PutMapping
    @RequirePermission("business:contract:update")
    public Result<Void> update(@Valid @RequestBody ContractDTO dto) {
        contractService.update(dto);
        return Result.success();
    }

    @DeleteMapping("/{id}")
    @RequirePermission("business:contract:delete")
    public Result<Void> delete(@PathVariable Long id) {
        contractService.delete(id);
        return Result.success();
    }
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/service/ContractService.java server/src/main/java/com/ems/module/business/controller/ContractController.java
git commit -m "feat: 合同管理 CRUD"
```

---

## 任务 9:报价 Service + Controller

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/service/QuoteService.java`
- 创建：`server/src/main/java/com/ems/module/business/controller/QuoteController.java`

- [ ] **步骤 1:创建 QuoteService**

```java
package com.ems.module.business.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.ems.common.PageResult;
import com.ems.common.exception.BusinessException;
import com.ems.module.business.dto.QuoteDTO;
import com.ems.module.business.entity.Quote;
import com.ems.module.business.mapper.QuoteMapper;
import com.ems.security.context.SecurityContext;
import org.springframework.beans.BeanUtils;
import org.springframework.stereotype.Service;

import java.time.LocalDate;

@Service
public class QuoteService {
    private final QuoteMapper quoteMapper;

    public QuoteService(QuoteMapper quoteMapper) {
        this.quoteMapper = quoteMapper;
    }

    public PageResult<Quote> page(long pageNum, long pageSize, String code, String customerName, String status) {
        LambdaQueryWrapper<Quote> wrapper = new LambdaQueryWrapper<Quote>()
                .like(code != null && !code.isEmpty(), Quote::getCode, code)
                .like(customerName != null && !customerName.isEmpty(), Quote::getCustomerName, customerName)
                .eq(status != null && !status.isEmpty(), Quote::getStatus, status)
                .orderByDesc(Quote::getCreateTime);
        IPage<Quote> page = quoteMapper.selectPage(new Page<>(pageNum, pageSize), wrapper);
        return PageResult.of(page.getRecords(), page.getTotal());
    }

    public Quote get(Long id) {
        Quote q = quoteMapper.selectById(id);
        if (q == null) throw new BusinessException("报价不存在");
        return q;
    }

    public void create(QuoteDTO dto) {
        Quote q = new Quote();
        BeanUtils.copyProperties(dto, q);
        if (dto.getQuoteDate() != null) q.setQuoteDate(LocalDate.parse(dto.getQuoteDate()));
        if (dto.getValidUntil() != null) q.setValidUntil(LocalDate.parse(dto.getValidUntil()));
        if (q.getBusinessType() == null) q.setBusinessType("NEW_BUILD");
        if (q.getBusinessId() == null) q.setBusinessId(dto.getProjectId());
        if (q.getStatus() == null) q.setStatus("DRAFT");
        if (q.getVersion() == null) q.setVersion(1);
        q.setCreateBy(SecurityContext.getUserId());
        quoteMapper.insert(q);
    }

    public void update(QuoteDTO dto) {
        Quote existing = get(dto.getId());
        BeanUtils.copyProperties(dto, existing);
        if (dto.getQuoteDate() != null) existing.setQuoteDate(LocalDate.parse(dto.getQuoteDate()));
        if (dto.getValidUntil() != null) existing.setValidUntil(LocalDate.parse(dto.getValidUntil()));
        quoteMapper.updateById(existing);
    }

    public void delete(Long id) {
        get(id);
        quoteMapper.deleteById(id);
    }
}
```

- [ ] **步骤 2:创建 QuoteController**

```java
package com.ems.module.business.controller;

import com.ems.common.PageResult;
import com.ems.common.Result;
import com.ems.module.business.dto.QuoteDTO;
import com.ems.module.business.entity.Quote;
import com.ems.module.business.service.QuoteService;
import com.ems.security.annotation.RequirePermission;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/business/quote")
public class QuoteController {
    private final QuoteService quoteService;

    public QuoteController(QuoteService quoteService) {
        this.quoteService = quoteService;
    }

    @GetMapping("/page")
    @RequirePermission("business:quote:list")
    public Result<PageResult<Quote>> page(@RequestParam(defaultValue = "1") long pageNum,
                                          @RequestParam(defaultValue = "10") long pageSize,
                                          String code, String customerName, String status) {
        return Result.success(quoteService.page(pageNum, pageSize, code, customerName, status));
    }

    @GetMapping("/{id}")
    @RequirePermission("business:quote:list")
    public Result<Quote> get(@PathVariable Long id) {
        return Result.success(quoteService.get(id));
    }

    @PostMapping
    @RequirePermission("business:quote:create")
    public Result<Void> create(@Valid @RequestBody QuoteDTO dto) {
        quoteService.create(dto);
        return Result.success();
    }

    @PutMapping
    @RequirePermission("business:quote:update")
    public Result<Void> update(@Valid @RequestBody QuoteDTO dto) {
        quoteService.update(dto);
        return Result.success();
    }

    @DeleteMapping("/{id}")
    @RequirePermission("business:quote:delete")
    public Result<Void> delete(@PathVariable Long id) {
        quoteService.delete(id);
        return Result.success();
    }
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/service/QuoteService.java server/src/main/java/com/ems/module/business/controller/QuoteController.java
git commit -m "feat: 报价管理 CRUD"
```

---

## 任务 10:设备台账 Service + Controller

**文件：**
- 创建：`server/src/main/java/com/ems/module/business/service/EquipmentService.java`
- 创建：`server/src/main/java/com/ems/module/business/controller/EquipmentController.java`

- [ ] **步骤 1:创建 EquipmentService**

```java
package com.ems.module.business.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.ems.common.PageResult;
import com.ems.common.exception.BusinessException;
import com.ems.module.business.dto.EquipmentDTO;
import com.ems.module.business.entity.Equipment;
import com.ems.module.business.mapper.EquipmentMapper;
import com.ems.security.context.SecurityContext;
import org.springframework.beans.BeanUtils;
import org.springframework.stereotype.Service;

import java.time.LocalDate;

@Service
public class EquipmentService {
    private final EquipmentMapper equipmentMapper;

    public EquipmentService(EquipmentMapper equipmentMapper) {
        this.equipmentMapper = equipmentMapper;
    }

    public PageResult<Equipment> page(long pageNum, long pageSize, String code, String name, String category, String status, Long projectId) {
        LambdaQueryWrapper<Equipment> wrapper = new LambdaQueryWrapper<Equipment>()
                .like(code != null && !code.isEmpty(), Equipment::getCode, code)
                .like(name != null && !name.isEmpty(), Equipment::getName, name)
                .eq(category != null && !category.isEmpty(), Equipment::getCategory, category)
                .eq(status != null && !status.isEmpty(), Equipment::getStatus, status)
                .eq(projectId != null, Equipment::getProjectId, projectId)
                .orderByDesc(Equipment::getCreateTime);
        IPage<Equipment> page = equipmentMapper.selectPage(new Page<>(pageNum, pageSize), wrapper);
        return PageResult.of(page.getRecords(), page.getTotal());
    }

    public Equipment get(Long id) {
        Equipment e = equipmentMapper.selectById(id);
        if (e == null) throw new BusinessException("设备不存在");
        return e;
    }

    public void create(EquipmentDTO dto) {
        Equipment e = new Equipment();
        BeanUtils.copyProperties(dto, e);
        if (dto.getCommissioningDate() != null) e.setCommissioningDate(LocalDate.parse(dto.getCommissioningDate()));
        if (dto.getWarrantyExpiry() != null) e.setWarrantyExpiry(LocalDate.parse(dto.getWarrantyExpiry()));
        if (e.getStatus() == null) e.setStatus("NORMAL");
        e.setCreateBy(SecurityContext.getUserId());
        equipmentMapper.insert(e);
    }

    public void update(EquipmentDTO dto) {
        Equipment existing = get(dto.getId());
        BeanUtils.copyProperties(dto, existing);
        if (dto.getCommissioningDate() != null) existing.setCommissioningDate(LocalDate.parse(dto.getCommissioningDate()));
        if (dto.getWarrantyExpiry() != null) existing.setWarrantyExpiry(LocalDate.parse(dto.getWarrantyExpiry()));
        equipmentMapper.updateById(existing);
    }

    public void delete(Long id) {
        get(id);
        equipmentMapper.deleteById(id);
    }
}
```

- [ ] **步骤 2:创建 EquipmentController**

```java
package com.ems.module.business.controller;

import com.ems.common.PageResult;
import com.ems.common.Result;
import com.ems.module.business.dto.EquipmentDTO;
import com.ems.module.business.entity.Equipment;
import com.ems.module.business.service.EquipmentService;
import com.ems.security.annotation.RequirePermission;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/business/equipment")
public class EquipmentController {
    private final EquipmentService equipmentService;

    public EquipmentController(EquipmentService equipmentService) {
        this.equipmentService = equipmentService;
    }

    @GetMapping("/page")
    @RequirePermission("business:equipment:list")
    public Result<PageResult<Equipment>> page(@RequestParam(defaultValue = "1") long pageNum,
                                              @RequestParam(defaultValue = "10") long pageSize,
                                              String code, String name, String category, String status, Long projectId) {
        return Result.success(equipmentService.page(pageNum, pageSize, code, name, category, status, projectId));
    }

    @GetMapping("/{id}")
    @RequirePermission("business:equipment:list")
    public Result<Equipment> get(@PathVariable Long id) {
        return Result.success(equipmentService.get(id));
    }

    @PostMapping
    @RequirePermission("business:equipment:create")
    public Result<Void> create(@Valid @RequestBody EquipmentDTO dto) {
        equipmentService.create(dto);
        return Result.success();
    }

    @PutMapping
    @RequirePermission("business:equipment:update")
    public Result<Void> update(@Valid @RequestBody EquipmentDTO dto) {
        equipmentService.update(dto);
        return Result.success();
    }

    @DeleteMapping("/{id}")
    @RequirePermission("business:equipment:delete")
    public Result<Void> delete(@PathVariable Long id) {
        equipmentService.delete(id);
        return Result.success();
    }
}
```

- [ ] **步骤 3:Commit**

```bash
git add server/src/main/java/com/ems/module/business/service/EquipmentService.java server/src/main/java/com/ems/module/business/controller/EquipmentController.java
git commit -m "feat: 设备台账 CRUD"
```

---

## 任务 11:菜单种子数据(业务模块权限)

**文件：**
- 修改:数据库执行 SQL 插入菜单

- [ ] **步骤 1:在数据库插入业务模块菜单与权限**

```sql
-- 业务管理目录
INSERT INTO sys_menu (id, parent_id, name, type, permission, path, icon, sort, status, create_time) VALUES
(200, 0, '业务管理', 1, NULL, '/business', 'Briefcase', 2, 1, NOW());

-- 业务管理子菜单
INSERT INTO sys_menu (id, parent_id, name, type, permission, path, icon, sort, status, create_time) VALUES
(201, 200, '项目管理', 2, 'business:project:list', '/business/project', 'FolderKanban', 1, 1, NOW()),
(202, 200, '合同管理', 2, 'business:contract:list', '/business/contract', 'FileText', 2, 1, NOW()),
(203, 200, '报价管理', 2, 'business:quote:list', '/business/quote', 'FileSpreadsheet', 3, 1, NOW()),
(204, 200, '设备台账', 2, 'business:equipment:list', '/business/equipment', 'Server', 4, 1, NOW());

-- 业务模块按钮权限(每模块 4 个:create/update/delete)
INSERT INTO sys_menu (id, parent_id, name, type, permission, sort, status, create_time) VALUES
(2011, 201, '项目新增', 3, 'business:project:create', 1, 1, NOW()),
(2012, 201, '项目编辑', 3, 'business:project:update', 2, 1, NOW()),
(2013, 201, '项目删除', 3, 'business:project:delete', 3, 1, NOW()),
(2021, 202, '合同新增', 3, 'business:contract:create', 1, 1, NOW()),
(2022, 202, '合同编辑', 3, 'business:contract:update', 2, 1, NOW()),
(2023, 202, '合同删除', 3, 'business:contract:delete', 3, 1, NOW()),
(2031, 203, '报价新增', 3, 'business:quote:create', 1, 1, NOW()),
(2032, 203, '报价编辑', 3, 'business:quote:update', 2, 1, NOW()),
(2033, 203, '报价删除', 3, 'business:quote:delete', 3, 1, NOW()),
(2041, 204, '设备新增', 3, 'business:equipment:create', 1, 1, NOW()),
(2042, 204, '设备编辑', 3, 'business:equipment:update', 2, 1, NOW()),
(2043, 204, '设备删除', 3, 'business:equipment:delete', 3, 1, NOW());

-- 给 admin 角色(id=1)分配所有业务菜单权限
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT 1, id FROM sys_menu WHERE id BETWEEN 200 AND 2043
ON DUPLICATE KEY UPDATE role_id = role_id;
```

- [ ] **步骤 2:Commit**

```bash
git add server/src/main/resources/db/schema.sql
git commit -m "feat: 业务模块菜单与权限种子数据"
```

---

## 任务 12:端到端验证

- [ ] **步骤 1:重启后端,确认启动无 Bean 创建失败**

运行:`cd server && mvn spring-boot:run`
预期:Started EmsApplication,无报错

- [ ] **步骤 2:用 admin token 测试 4 个模块的分页接口**

```powershell
# 先登录拿 token
$login = Invoke-WebRequest -Uri http://localhost:8080/api/auth/login -Method Post -Body '{"username":"admin","password":"admin123"}' -ContentType "application/json" -UseBasicParsing
$token = ($login.Content | ConvertFrom-Json).data.token
$h = @{ Authorization = "Bearer $token" }

# 测试 4 个分页接口
Invoke-WebRequest -Uri http://localhost:8080/api/business/project/page -Headers $h -UseBasicParsing
Invoke-WebRequest -Uri http://localhost:8080/api/business/contract/page -Headers $h -UseBasicParsing
Invoke-WebRequest -Uri http://localhost:8080/api/business/quote/page -Headers $h -UseBasicParsing
Invoke-WebRequest -Uri http://localhost:8080/api/business/equipment/page -Headers $h -UseBasicParsing
```
预期:4 个接口均返回 `{"code":200,...,"data":{"list":[],"total":0}}`

- [ ] **步骤 3:测试新增 + 查询链路**

```powershell
# 新增项目
$body = '{"code":"P001","name":"测试项目","customerName":"测试客户","type":"NEW_BUILD","status":"DRAFT"}'
Invoke-WebRequest -Uri http://localhost:8080/api/business/project -Method Post -Body $body -Headers $h -ContentType "application/json" -UseBasicParsing

# 查询,应返回 1 条
Invoke-WebRequest -Uri http://localhost:8080/api/business/project/page -Headers $h -UseBasicParsing
```
预期:新增返回 200,分页返回 total:1

- [ ] **步骤 4:验证无 token → 401,无权限用户 → 403**

```powershell
# 无 token
Invoke-WebRequest -Uri http://localhost:8080/api/business/project/page -UseBasicParsing
# 预期 401
```

- [ ] **步骤 5:Commit 验证记录**

```bash
git commit --allow-empty -m "chore: 业务模块 2A 端到端验证通过"
```

---

## 范围说明(本计划不做)

以下留给后续计划:
- 维护型项目编排(模块五):MaintenanceProject/MaintenancePoint 实体与流程 → 2B
- 季度结算定时任务与结算单 → 2C
- 项目进度管理(模块三):Progress/ProgressNode/进度上报 → 2D
- 项目验收(模块三):Acceptance 实体与验收流程 → 2E
- 文件上传(合同扫描件、报价单、设备说明书)→ 通用能力 2F
- 审批流(合同审批、报价审批)→ 通用能力 2G
- 前端业务页面 → 前端 1C

---

## 自检

**规格覆盖度:** 需求文档模块一(合同)、模块二(报价)、模块四的设备台账(3.4.1)、模块六数据模型中的 Project/Contract/Quote/Equipment 实体均已覆盖。模块三进度、模块五维护型项目、模块四维保任务/合同/记录按范围说明留给后续。

**类型一致性:** DTO 字段名与 entity 字段名一一对应(BeanUtils.copyProperties);Controller 路径 `/business/{module}` 与权限标识 `business:{module}:*` 一致;PageResult 字段 `list` 与 1A 一致。

**占位符扫描:** 无 TODO/待定,所有步骤含完整代码。
